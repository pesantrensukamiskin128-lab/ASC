[phases.setup]
nixPkgs = ['php83', 'php83Extensions.pdo_mysql', 'php83Extensions.mbstring', 'php83Extensions.xml', 'php83Extensions.tokenizer', 'php83Extensions.bcmath', 'php83Extensions.zip', 'php83Extensions.gd', 'php83Extensions.exif', 'php83Extensions.fileinfo', 'php83Extensions.curl', 'php83Extensions.intl', 'composer']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader --no-interaction']

[phases.build]
cmds = [
  'php artisan config:cache',
  'php artisan route:cache',
  'php artisan view:cache',
]

[start]
cmd = 'php artisan migrate --force && php artisan storage:link --force && php artisan queue:restart && php -S 0.0.0.0:$PORT -t public'
